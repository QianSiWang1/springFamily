spring:
  application:
    name: nacos-pig-service
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
        namespace: dev
        username: nacos
        password: nacos
      config:
        namespace: dev
        group: DEFAULT_GROUP
        file-extension: yml
        server-addr: localhost:8848
        username: nacos
        password: nacos
        refresh-enabled: true
#        shared-configs[0]:
#          data-id: tulingmall-nacos.yml
#          group: DEFAULT_GROUP
#          refresh: true
#        shared-configs[1]:
#          data-id: tulingmall-db-common.yml
#          group: DEFAULT_GROUP
#          refresh: true